import{describe,it,expect}from'vitest';
import fs from'node:fs';import vm from'node:vm';
const source=fs.readFileSync('foundry-module/scripts/main.js','utf8');
function load(){const sandbox={console,document:{createElement:()=>({textContent:'',get innerHTML(){return this.textContent}})},Hooks:{once(){},on(){}},game:{modules:{get:()=>({})}},ui:{notifications:{info(){},error(){}}},CONST:{DOCUMENT_OWNERSHIP_LEVELS:{OBSERVER:2,NONE:0}}};vm.createContext(sandbox);vm.runInContext(source,sandbox);return sandbox}
describe('Foundry companion standalone parser',()=>{it('loads without syntax errors',()=>expect(()=>load()).not.toThrow());it('splits markdown headings into lore entries',()=>{const s=load(),p=s.plainLorePackage('World','# Coral\nCapital city.\n# Port Stell\nOld harbor.');expect(p.entries).toHaveLength(2);expect(p.entries.map(e=>e.name)).toEqual(['Coral','Port Stell'])});it('keeps headingless text as one journal',()=>{const s=load(),p=s.plainLorePackage('Notes','A hidden shrine beneath the harbor.');expect(p.entries).toHaveLength(1);expect(p.entries[0].name).toBe('Notes')});it('validates native packages',()=>{const s=load(),p=s.plainLorePackage('Lore','text');expect(s.validatePackage(p)).toBe(true);expect(()=>s.validatePackage({entries:[]})).toThrow()});it('previews journals without Foundry documents',()=>{const s=load(),p=s.plainLorePackage('Lore','# One\na\n# Two\nb');const r=s.previewPackage(p);expect(r.Journal).toBe(2);expect(r.Actor).toBe(0)})});
