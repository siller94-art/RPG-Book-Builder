import type{CampaignProject,ContentEntry}from"./models";export function downloadJson(data:unknown,name:string){const blob=new Blob([JSON.stringify(data,null,2)],{type:"application/json"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=name;a.click();URL.revokeObjectURL(url)}export function exportEntry(entry:ContentEntry){downloadJson({format:"rpg-book-builder-entry",version:1,entry},`${entry.kind}-${entry.name.replace(/[^a-z0-9]+/gi,"-").toLowerCase()}.json`)}export async function importProjectFile(file:File):Promise<CampaignProject>{const data=JSON.parse(await file.text());if(data?.schemaVersion!==1||!Array.isArray(data.entries))throw new Error("Unsupported project file");return data as CampaignProject}