export async function makeToken(source:string,ring="#7b302a",size=512):Promise<string>{const img=new Image();img.src=source;await img.decode();const c=document.createElement("canvas");c.width=c.height=size;const x=c.getContext("2d")!;x.clearRect(0,0,size,size);x.save();x.beginPath();x.arc(size/2,size/2,size*.43,0,Math.PI*2);x.clip();const scale=Math.max(size*.86/img.width,size*.86/img.height);const w=img.width*scale,h=img.height*scale;x.drawImage(img,(size-w)/2,(size-h)/2,w,h);x.restore();x.strokeStyle=ring;x.lineWidth=size*.07;x.beginPath();x.arc(size/2,size/2,size*.465,0,Math.PI*2);x.stroke();return c.toDataURL("image/png")}export function downloadDataUrl(url:string,name:string){const a=document.createElement("a");a.href=url;a.download=name;a.click()}